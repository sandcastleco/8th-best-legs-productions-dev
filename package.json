{
  "name": "8th-best-legs-productions",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && echo 8thbestlegs.biz > ./dist/CNAME && node deploy.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "gh-pages": "^6.1.1",
    "nuxt": "^3.12.3"
  },
  "dependencies": {
    "bootstrap": "^5.2.3",
    "eslint": "^9.6.0",
    "nuxi": "^3.12.0",
    "sass": "^1.62.1"
  }
}
